import Web3 from 'web3';
import Composer from './composer';
import { Registrations, Options } from './types';
export default class extends Composer implements Registrations {
    blockchainApi: Web3 | any;
    /**
     *
     * @param blockchainApi - current Web3 or Rsk3 instance
     * @param registry - RNS Registry Web3 Contract instance
     */
    constructor(blockchainApi: Web3 | any, options?: Options);
    private _createTldOwner;
    private _searchAvailabilityByLabel;
    private _searchAvailabilityByDomain;
    available(domain: string): Promise<boolean | string[]>;
}
